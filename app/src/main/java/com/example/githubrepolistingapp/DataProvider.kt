package com.example.githubrepolistingapp

object DataProvider {
    val repoList = listOf(
        Repo(
            name = "MazenRepo",
            description = "MyRepo",
            htmlUrl = "www"
        ),
        Repo(
            name = "MazenRepo2",
            description = "MyRepo2",
            htmlUrl = "www"
        ),
        Repo(
            name = "MazenRepo3",
            description = "MyRepo3",
            htmlUrl = "www"
        )
    )
}